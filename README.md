# C++26 Dependency Inection

This is an attempt at making a dependency injector using reflection.

## TODO

  - [ ] create object in get<>()
  - [ ] support creating arbitrary objects with inject<> properties from container
  - [ ] support arguments in builder.add_*() to pass into creation in get<>()
  - [ ] add support for singletons
  - [ ] add support for scopes
  - [ ] add support for chaining containers

